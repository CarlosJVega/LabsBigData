resources:
  jobs:
    crypto_summary_job:
      name: crypto_summary_job
      tasks:
        - task_key: crypto_summary_task
          notebook_task:
            notebook_path: ../notebooks/01_crypto_summary.py
            base_parameters:
              catalog: ${var.catalog}
              schema: ${var.schema}
              source_table: ${var.source_table}
              output_table: ${var.output_table}